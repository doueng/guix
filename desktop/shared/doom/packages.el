;;; packages.el --- Custom packages -*- lexical-binding: t; -*-
;;
;; This file is for adding custom packages or overriding existing ones.
;; The Doom framework and packages are installed at runtime via straight.el
;; (see the doom-emacs feature README), so every package used by
;; the config must be declared here with `package!`.

(package! consult-gh)
